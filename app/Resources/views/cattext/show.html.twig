{% extends 'base.html.twig' %}

{% block body %}
    <h1>catText</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catText.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ catText.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_categorie2textes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_categorie2textes_edit', { 'id': catText.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
